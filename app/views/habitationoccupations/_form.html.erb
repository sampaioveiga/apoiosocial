<%= form_for @habitationoccupation, html: { class: 'form-horizontal' } do |f| %>
	<% if @habitationoccupation.errors.any? %>
		<div class="error_explanation">
			<h2>O formulário contém <%= pluralize(@habitationoccupation.errors.count, "erro") %></h2>
			<ul>
				<% for message in @habitationoccupation.errors.full_messages %>
					<li><%= message %>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group">
		<%= f.label :nome, class: 'col-md-3 control-label' %>
		<div class="col-md-9">
			<%= f.text_field :nome, class: 'form-control' %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-md-offset-3 col-md-3">
			<%= f.submit "Guardar", class: 'btn btn-success' %>
		</div>
		<div class="col-md-3">
			<%= link_to "Cancelar", habitationoccupations_path, class: 'btn btn-link' %>
		</div>
	</div>
<% end %>