<%= form_for @habitationtype, html: { class: 'form-horizontal' } do |f| %>
	<% if @habitationtype.errors.any? %>
		<div class="error_explanation">
			<h2>O formulário contém <%= pluralize(@habitationtype.errors.count, "erro") %></h2>
			<ul>
				<% for message in @habitationtype.errors.full_messages %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group">
		<%= f.label :nome, class: 'col-sm-2 control-label' %>
		<div class="col-sm-6">
			<%= f.text_field :nome, class: 'form-control' %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-sm-offset-2 col-sm-3">
			<%= f.submit "Guardar", class: 'btn btn-success' %>
		</div>
		<div class="col-sm-3">
			<%= link_to "Cancelar", habitationtypes_path, class: 'btn' %>
		</div>
	</div>
<% end %>